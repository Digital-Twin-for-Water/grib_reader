# KMA DFS GRIB 시계열 추출 — 필수 패키지
# 설치: pip install -r requirements.txt
pygrib
numpy>=1.21
pandas>=1.3
matplotlib>=3.4
notebook

# read_grib_xarray 사용 시 (conda-forge 권장)
# conda install -c conda-forge eccodes cfgrib
# pip install xarray
